<odoo>
    <record id="sale_order_view_form" model="ir.ui.view">
        <field name="name">sale.order.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button type="object" string="Open" name="open_record" invisible="state in ('draft','sent','open','cancel')"/>
                <button type="object" string="Close"  name="close_record" invisible="state in ('draft','sent','close','cancel')"/>
            </xpath>
            <xpath expr="//field[@name='state']" position="replace">
                  <field name="state" widget="statusbar" statusbar_visible="draft,sent,sale,open,close,cancel"/>
            </xpath>
        </field>
    </record>
</odoo>